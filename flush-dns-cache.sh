#Flush the DNS Server cache
#
#1. Emable the SSH service via the web interface (Control Panel -> Terminal & SNMP).
#2. Connnect to the server via SSH
#3. Run this command...

sudo /volume1/@appstore/DNSServer/script/flushcache.sh

#4. Disable the SSH service again via the web interface (Control Panel -> Terminal & SNMP).
